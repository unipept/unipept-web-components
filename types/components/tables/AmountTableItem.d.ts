export default class AmountTableItem {
    readonly count: number;
    readonly relativeCount: number;
    readonly name: string;
    readonly code: string;
    constructor(count: number, relativeCount: number, name: string, code: string);
}
